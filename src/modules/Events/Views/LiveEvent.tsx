import React from 'react';
import useLiveEventViewModel from "../ViewModels/Events/LiveEventViewModel";

const LiveEvent = (props: any) => {
    // const view = useLiveEventViewModel({props});
    //
    // console.log(view);
    return <h1>hello world</h1>
};
export default LiveEvent;
